version = '0.1-SNAPSHOT'

dependencies {
//  compile 'org.apache.bigtop.itest:bigtop-smokes:0.8.0'
  if (!project.hasProperty('apps'))
    compile project(':base')
}

mainClassName = "earay.bigdata.bigtop.BigtopApplication"
